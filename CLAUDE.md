# 콘텍스트 조정 (kind)
조정하려는 두 단어사이에 **공백 여부**가 `kind` 의 판단 근거
- 공백 없이 붙어있음: `cnt`
- 공백 있음: `space`

# 콘텍스트 조정 최적화
최소한의 호출로 원하는 조정 의도 달성

## 일괄 조정보다 단계별 진행
2개 이상 동시 조정 금지.
1회 호출 후 재분해하여 추가 조정 여부 판단한다

## 조정 목적
의도한 분해 구조를 첫번째 **(\[0\]번째 인덱스)** 에 위치시키는 것.
즉 점수는 동점이든 1점차이든 중요하지 않음

## 타입 보정
`ContextCore` 소스 참고

타입 보정을 받을 수 있는 경우라면 콘텍스트 조정보다 타입 보정을 우선한다

단어들 중 `학습%` 타입 단어는 필요하다면 적절한 타입으로 변경하여 보정 효과를 노릴 수 있다

# 분해 구조 정합 검증

## 판단 근거
- `type`, `memo` 에 적힌 정보
- `결합` 타입인 경우 그 결합의 구성 단어쌍의 `memo`

## 단어 누락
- 문장 분해 실패하여 `500` 반환 > 단어 조회 > 등록 (없으면)
- 분해 성공 > 메모 정합 판단 > (메모가 의도와 다르면) > 동음이의어 조회 > 등록 (없으면)

## 단어 surface
단어가 등록되어 있어도 문장 구조가 1~후순위 에 surface 되지 않을 수도 있다.
이는 조정을 통해 surface 시킬 수 있으므로 조정을 통하여 올바른 문장 구조로 유도한다

# 명령어
인라인 파이썬, 파워쉘 로직 등 사용 금지
도구화 된 스크립트만 사용하고 필요한 경우 담당자에게 검토받아 도구 수정하여 사용
