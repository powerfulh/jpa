server:
  port: 8079

spring:
  datasource:
    driver-class-name: org.mariadb.jdbc.Driver
    url: ${MARIADBDOMAIN}
    username: powerfulh
    password: ${MARIADBPASSWORD}

logging:
  pattern:
    console: ${lpc:%d{MMdd HH:mm:ss} %-5level %logger{36} - %msg [%thread]%n}